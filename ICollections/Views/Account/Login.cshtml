@model ICollections.ViewModels.LoginViewModel

<h2 class="lng-LogIn theme-main-text">Log in!</h2>
<h4 class="theme-main-text"><i>You don't have an account?</i></h4>
<a class = "text-black-50 lng-RegisterLoginPage theme-main-text" asp-controller="Account" asp-action="Register">Register!</a>
<form asp-action="Login" asp-controller="Account" asp-anti-forgery="true">
    <div class="validation" asp-validation-summary="ModelOnly"></div>
    <div>
        <div class="form-group">
            <label class="lng-EnterEmailLogin" asp-for="Email" id="text">Enter email:</label>
            <input type="text" class="form-control" asp-for="Email" placeholder="example@example.com"/>
            <span asp-validation-for="Email"></span>
        </div>
        <div class="form-group">
            <label class="lng-EnterPasswordLogin" asp-for="Password" id="text">Enter password:</label>
            <input asp-for="Password" class="form-control" placeholder="********"/>
            <span asp-validation-for="Password"></span>
        </div>
        <div>
            <label class="lng-RememberMe" asp-for="RememberMe" id="text"></label>
            <input asp-for="RememberMe" />
        </div>
        <div class="form-group">
            <button type="submit" class="btn btn-outline-dark lng-InputLogin">Login</button>
        </div>
    </div>
</form>

<script src="/js/ChangeLanguage.js"></script>
<script src="/js/ChangeTheme.js"></script>

<script>
    let pageTitle = (getCurrentLanguage() === 'en') ? 'Welcome Page - ICollections' : 'Добро пожаловать - ICollections';
         
    document.querySelector('title').innerHTML = pageTitle;
</script>

